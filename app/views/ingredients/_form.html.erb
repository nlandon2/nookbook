<%= form_for([@recipe, @recipe.ingredients.build]) do |f| %>
 <%= f.label "Add ingredients:" %>
 <%= f.text_field :name, placeholder: "New Ingredient" %>
 <%= f.text_field :qty, placeholder: "Quantity" %>
 <%= f.text_field :unit, placeholder: "Unit" %>
 <%= f.submit %>
<% end %>